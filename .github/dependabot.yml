# Update action SHAs. The MSRV comes from Cargo.toml, independently of the
# rust-toolchain action revision. Cargo advisories are checked by the audit workflow.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: CI
